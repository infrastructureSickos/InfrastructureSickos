# Organizations

This directory mirrors GitHub repositories organized by org/user namespace.
Each subdirectory is a GitHub **organization or user** name (e.g. `infrastructureSickos/`).
The repos themselves live one level deeper: `Organizations/<org>/<repo-name>/`.

Do NOT place repos directly in this folder — always nest them under their org/user subdirectory.
